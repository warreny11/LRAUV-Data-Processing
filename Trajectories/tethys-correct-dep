fid = fopen('measured_parameter-6tethys.csv');
for i = 1:1000000
tline = fgetl(fid);

idx_63 = find(tline==',',4);
tysdepstr = tline(idx_63(3)+1 : idx_63(4)-1);
tysdepbig(i) = str2num(tysdepstr);

end

indices = find(tysdepbig<0.5);%should find all values less than 0.5 and delete them
tysdepbig(indices) = NaN;

